import { Card } from "antd"

const UpcomingEvents = () => {
    return (
        <Card style={{ height: "100%" }} headStyle={{ padding: "8px 16px" }}
            bodyStyle={{ padding: "0 1rem" }}
            title={<div style={{ display: "flex", alignItems: "center", gap: "8px" }}>
            </div>}>
            UpcomingEvents
        </Card>
    )
}

export default UpcomingEvents